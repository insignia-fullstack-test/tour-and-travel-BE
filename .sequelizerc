const dotenv = require('dotenv');
const path = require('path');

// To read dotenv during the migration
dotenv.config()

module.exports = {
  'config': path.resolve('config', 'database', 'database.js'),
  'migrations-path': path.resolve('config', 'database', 'migrations'),
  'models-path': path.resolve('config', 'database', 'models'),
  'seeders-path': path.resolve('config', 'database', 'seeders'),
}
